---
name: 운동기록
description: 애플 피트니스(Apple Fitness/Health) 운동 요약 스크린샷을 받아서 데이터를 파싱하고, 노션 월별 페이지의 운동 기록 테이블에 자동으로 추가한 뒤 트레이너 피드백을 노션 테이블과 대화에 모두 제공하는 스킬. 사용자가 /운동기록 을 입력하거나 운동 캡처/스크린샷을 첨부하며 노션에 기록해달라고 하면 반드시 이 스킬을 사용할 것. "운동 기록해줘", "노션에 추가해줘", "피트니스 사진 올릴게" 등의 표현에도 트리거.
---

# 운동기록 스킬

사용자가 애플 피트니스 운동 요약 화면 이미지를 첨부하면, 데이터를 파싱해서 노션 월별 페이지에 자동 기록하고 트레이너 피드백을 노션과 대화창에 모두 제공한다.

## 노션 구조

사용 전 아래 ID를 본인 노션 페이지 ID로 교체한다.

```
운동 (루트)  →  ROOT_PAGE_ID
  └ 2026년   →  YEAR_PAGE_ID
       └ 2026년_06  →  월별 페이지 (자동 생성)
       └ 2026년_07  (없으면 새로 생성)
       └ ...
```

## 테이블 컬럼 구조

| 날짜 | 운동 종류 | 시간 | 강도 | 평균BPM | 최고BPM | 칼로리 | 체중 | 메모 | 트레이너 피드백 |

- **메모**: 사용자가 이미지 외에 텍스트로 입력한 내용 전체를 여기에 취합
- **트레이너 피드백**: 생성한 피드백 전문을 이 컬럼에도 기록

## 실행 순서

### 1단계: 이미지 파싱 + 메모 수집

**이미지에서 추출할 항목:**

| 항목 | 추출 방법 |
|------|----------|
| 날짜 | 화면 상단 날짜 → `M/D(요일)` 형식 (예: `6/1(월)`) |
| 운동 종류 | 활동명을 짧게 정리. 매핑 예시: 실내 사이클링→`사이클`, 달리기/런닝→`런닝`, 걷기→`걷기`, 수영→`수영`, 근력 운동→`웨이트`, 필라테스→`필라테스`, 요가→`요가`. 이미지에 표시된 활동명을 그대로 사용하되 너무 길면 줄임. |
| 시간 | 운동 시간 분 단위. 0:30:36 → `30분`, 1:05:00 → `65분` |
| 강도 | 운동 강도 숫자 + 텍스트 (예: `6(보통)`) |
| 평균BPM | 평균 심박수 숫자 |
| 최고BPM | 심박수 그래프 최고값 |
| 칼로리 | **총 킬로칼로리** (활동 칼로리 아닌 총합) |
| 체중 | 사용자가 텍스트로 알려준 경우에만. 없으면 `-` |

**메모 수집:**
이미지 외에 사용자가 텍스트로 입력한 내용이 있으면 전부 메모 컬럼에 넣는다. 없으면 비워둠.

### 2단계: 여러 운동 종합 처리

사용자가 같은 날 여러 이미지를 올리거나 여러 운동을 했다고 알려주면:
- **한 행에 종합** 입력 (날짜 기준으로 합산)
- 운동 종류: `사이클+런닝` 처럼 `+`로 연결
- 시간: 합산 (예: `30분+20분=50분`)
- 칼로리: 합산
- BPM: 각 운동 중 가장 높은 최고BPM 사용, 평균BPM은 가중평균 또는 평균
- 강도: 가장 높은 강도 기준

### 3단계: 노션 월별 페이지 확인/생성

1. 날짜에서 월(MM)을 추출 (예: 6월 → `06`)
2. `notion-fetch`로 연도 페이지 조회 → `YYYY년_MM` 페이지 존재 확인
3. **없으면**: `notion-create-pages`로 연도 페이지 하위에 새 페이지 생성 (아래 초기 테이블 포함)

새 페이지 초기 테이블:
```
| 날짜 | 운동 종류 | 시간 | 강도 | 평균BPM | 최고BPM | 칼로리 | 체중 | 메모 | 트레이너 피드백 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
```

### 4단계: 기존 페이지 테이블 확인 및 행 추가

`notion-fetch`로 해당 월 페이지를 조회한 뒤:

1. **컬럼 수 확인**: 테이블 헤더에 `트레이너 피드백` 컬럼이 없으면 추가. 기존 행에도 빈 셀 추가.
2. **중복 날짜 확인**: 이미 같은 날짜 행이 있으면 새 행 추가 대신 해당 행을 업데이트.
3. **새 행 추가**: 없으면 **테이블 맨 마지막 행 뒤에** 추가. 날짜 오름차순이 유지되도록 기존 행 순서를 절대 바꾸지 말 것. `update_content`로 마지막 `</tr>` 바로 앞 행을 기준으로 새 행을 뒤에 삽입한다.

> ⚠️ 새 행은 반드시 테이블의 **맨 아래(마지막 데이터 행 뒤)**에 추가한다. 기존 행 사이에 끼워 넣으면 안 된다.

### 5단계: 이전 기록 수집

피드백 작성 전에 노션에서 과거 운동 데이터를 수집한다.

**수집 절차:**

1. 이미 3단계에서 fetch한 현재 월 페이지의 테이블에서 이번 기록을 제외한 모든 행을 읽는다.
2. 현재 월 기록이 **3회 미만**이면 (월 초 첫 기록 포함), 연도 페이지를 fetch해서 직전 달 페이지 URL을 찾고 해당 페이지도 fetch한다. 직전 달 테이블의 **마지막 행부터 역순으로** 부족한 만큼 추가로 읽는다.
3. 최종적으로 이번 기록 제외 **직전 3~5회** 운동 데이터를 확보한다 (날짜, 운동 종류, 시간, 강도, 평균BPM, 최고BPM, 칼로리, 체중).
4. 전체 기록이 없는 진짜 첫 기록이면 비교 없이 오늘 데이터만으로 피드백 작성.

이 덕분에 월이 바뀌어도 피드백 비교가 자연스럽게 이어진다.

### 6단계: 트레이너 피드백 생성

이전 기록과 오늘 기록을 비교해서 피드백을 작성한다. 단순한 오늘 평가가 아니라 **추세와 변화**에 초점을 맞춘다.

**사용자 정보 (커스터마이즈 필요)**
- 키, 현재 체중, 목표 체중
- 운동 종류 및 빈도
- 인터벌 구성
- 식단 방향

**BPM 존 기준:**
- ~120: 지방 연소 존 (저강도)
- 120~140: 유산소 존 (적정)
- 140~160: 심폐 강화 존 (고강도, 다이어트 효과적)
- 160+: 최대 심박 존 (매우 고강도)

**피드백 구성 (4~6문장, 이전 기록과 비교 중심):**

1. **오늘 세션 평가** — BPM 존, 칼로리, 강도를 간략히 평가
2. **직전 운동 대비 변화** — 평균BPM / 칼로리 / 강도 증감과 그 의미 해석
3. **최근 N회 추세** — 칼로리 증감, BPM 변화, 체중 변화 흐름 언급 (데이터 있을 때)
4. **체중 진행률** — 체중 데이터가 있을 때 목표 대비 진행 상황 + 현재 추세 기반 예상 시점
5. **다음 운동 어드바이스** — 추세를 고려한 구체적 제안 (이전 피드백과 중복되지 않게)
6. **격려** — 구체적 수치를 근거로 한 진심 어린 한마디

이전 기록이 없는 첫 기록이라면 비교 항목은 생략하고 오늘 데이터 기반으로만 작성한다.

피드백은 대화창과 노션 `트레이너 피드백` 컬럼 양쪽에 동일하게 입력한다.

### 7단계: 노션 업데이트 실행

`notion-update-page`의 `update_content` 명령으로 해당 월 페이지를 업데이트한다.

업데이트 시 반드시:
- 페이지 상단의 기존 텍스트(프로필 정보 등) 유지
- 기존 테이블 모든 행 유지 및 순서 보존
- 새 행은 테이블 맨 마지막에 추가
- `트레이너 피드백` 컬럼에 피드백 텍스트 입력

## 출력 형식

```
✅ 노션 기록 완료! (YYYY년_MM 페이지)

📊 기록된 데이터:
- 날짜: 6/1(월)
- 운동: 사이클 30분
- 강도: 6(보통) | 평균BPM: 145 | 최고BPM: 167
- 칼로리: 348kcal | 체중: 88.2kg
- 메모: [있으면 표시]

💬 트레이너 피드백:
[피드백 내용]
```
